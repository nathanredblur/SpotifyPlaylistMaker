# Spotify API Configuration
# Get your client ID from: https://developer.spotify.com/dashboard

# Public variables (accessible in client-side code)
# In Astro, variables prefixed with PUBLIC_ are exposed to the client
PUBLIC_SPOTIFY_CLIENT_ID=your_spotify_client_id_here

# Redirect URIs (adjust based on your deployment)
# IMPORTANT: These must point to /app to handle the OAuth callback
# Also add these exact URLs in your Spotify App settings
PUBLIC_SPOTIFY_REDIRECT_URI_LOCAL=http://localhost:4321/app
PUBLIC_SPOTIFY_REDIRECT_URI_REMOTE=https://your-domain.com/app
